services:
  - type: web
    name: sensorcalibrationplatform
    env: python
    plan: free
    rootDir: Backend/calibration_platform
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn calibration_platform.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
